[package]
name = "zeus"
version = "0.1.0"
edition = "2021"

[profile.dev]
panic ="abort"

[profile.release]
panic ="abort"

[dependencies]
spin = "0.9.8"
bitvec = { version = "1.0.1", default-features = false, features = ["alloc"] }


